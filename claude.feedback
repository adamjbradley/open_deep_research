
---

## Round 2 — 2026-06-12 — review of v2 revision

Internal Claude red team (same three lenses), focused on whether the v2 fixes held.

### Reviewer A — Product Skeptic — convergence call: YES (advance)
- Fork A held but the rendering contract is a hidden behavioral promise P5 can't guarantee.
- Fork C is the strongest fix. Fork B coherent but "regenerated from claims" is unfalsifiable.
- **New contradiction (key):** auto-merge (P1) + trusted-only synthesis (P7) means a brand-new
  subject's first report synthesizes from ZERO trusted claims — the cold-start case §1 promises to
  serve. Gating created this; v1 didn't have it.
- Also: P7 has no promotion *trigger* (corroboration only arrives on later runs; nothing triggers
  re-research) — "the accumulation flywheel has no motor." Single-researcher + subscription ToS
  caps the wedge. Domain-profile bias named but unowned.
- **Single highest-value change:** define what a first-run report on a brand-new subject
  synthesizes from.

### Reviewer B — Technical Realist — convergence call: YES (advance)
- Fork A holds technically. Fork B inconsistent: prose→claims→prose is NOT identity-preserving
  (extraction is lossy); "regenerated" prose is a thinner artifact — say "claims-derived view."
- **Fork C's gate has no feasible promotion signal:** P7 promotes on corroboration, but P4 concedes
  corroboration can't detect independence and is "a weak signal." So promotion is either inflatable
  (defeats the gate) or reduces to single-source-trust + no-contradiction. v2 never picks. The gate
  is called "primary defense" while its main signal is conceded unsound — the central gap.
- **New contradiction:** "provisional excluded from synthesis" (P7) vs "conflict surfaced inline in
  prose" (P5) — a fresh contradicting claim is provisional, so is it excluded or surfaced?
- Cold-start: trusted-by-default makes first reports empty — early value is *negative*.
- **Single highest-value change:** define the promotion signal — e.g. promote = (source-trust ≥ bar)
  AND (no open contradiction), corroboration explicitly NOT an input until independence lands; and
  state whether provisional contradicting claims surface in prose.

### Reviewer C — Market Cynic — convergence call: NO
- §7 names the "why switch" question then declines to answer it; the wedge loses to Elicit (rigor)
  and ChatGPT-memory (low-effort recall). The defensible sliver — cross-run conflict/confidence
  state — is exactly what Fork A defers (no UI to see it).
- **Fork A guts the v1 product:** v1 ships "a schema plus a ⚠ string in prose" — "a database
  migration wearing a vision's clothes." Fork C adds friction with no v1 payoff (thinner reports).
- No single-session "aha"; audit dependency unfunded; target persona unspecified for GTM.
- **Single highest-value change:** pull ONE read-only surface into v1 — a `dossier show <subject>`
  render of trusted claims, open conflicts, and confidence tiers — so the wedge is testable in the
  first session, not a deferred promise.

---

## Round 3 — 2026-06-12 — convergence check on v3

Internal Claude red team (convergence check). Tally with external CLIs: **3 ADVANCE / 2 ANOTHER ROUND**.

### Reviewer A — Product Skeptic — ANOTHER ROUND
- (1) degraded synthesis RESOLVED; (2) promotion rule RESOLVED; (3) reframed model RESOLVED.
- New issues: P7 "no open contradiction" depends on deferred contradiction detection (blocker —
  flag the dependency or define v1 fallback); §3 metric "share of trusted claims backed by a
  trusted source" is ~tautological under P7; machine-generated caveats are load-bearing but deferred.

### Reviewer B — Technical Realist — ADVANCE
- All three collisions RESOLVED. Promotion rule implementable as written; the contradiction-detection
  dependency is *acceptable at the vision layer* (commits to what the gate decides on, not mechanism;
  P5 already makes conflicts first-class). Remaining items sub-vision-layer / Architecture's problem.

### Reviewer C — Market Cynic — ADVANCE
- Round-2 "no testable product" RESOLVED by the read-only `dossier show` view + degraded synthesis;
  the wedge is now demonstrable in session one. Two residual GTM concerns (CLI is a thin vehicle for
  the wedge; session-one shows the weakest version of the differentiator) are Feature-Spec/sequencing
  risks, not unsettled vision questions.

### External CLIs
- **codex — ANOTHER ROUND:** (1)(2) RESOLVED, (3) PARTIAL. Blocker: immutable run reports
  ("never rewritten") vs. P5 "surface conflicts wherever rendered (run reports, dossier view)" —
  reports can't be both frozen artifacts and live surfaces; plus "confidence-gated ingestion" vs
  P1 "ingestion is unconditional" terminology.
- **gemini — ADVANCE:** all collisions resolved; no new contradictions; structurally sound.

### Disposition (v4)
All round-3 findings are precision/wording fixes, not structural. Applied in v4: run reports are
immutable snapshots (current conflicts live in the dossier view); rendering contract scoped to
claim-derived rendering; "gated promotion" terminology; honest contradiction-detection dependency
flag; success metric de-tautologized. **Convergence reached** — remaining feedback is operational,
not structural.

---

## Round 4 — 2026-06-12 — final verification of v4

Final check. Tally with external CLIs: **4 ADVANCE / 1 ANOTHER ROUND**.

- **Product skeptic — ADVANCE.** All five v4 fixes (a)-(e) hold; no fix reopened a prior issue.
- **Technical realist — ADVANCE.** Each fix holds logically; (b)'s carve-out reinforces immutability.
- **Market cynic — ADVANCE.** §7 wedge intact and tightened; de-tautologized §3 now measures the
  wedge rather than restating it; no new GTM contradiction.
- **gemini — ADVANCE.** Structural and terminology convergence reached.
- **codex — ANOTHER ROUND (single sharp objection):** v4 fix (d) "DOESN'T HOLD" — degraded-synthesis
  caveats mark *provisional* content, so they cannot backstop an undetected contradiction that
  *over-promotes* a claim to trusted (such a claim renders trusted/uncaveated). Only audit (and later
  detection→demotion) does. **Valid.**

### Disposition (v5)
codex is right. Fixed in v5: P7 now separates the two failure modes — over-promotion is backstopped
by after-the-fact audit and later contradiction-detection→demotion (NOT caveats); caveats backstop
the distinct degraded-synthesis (thin-trusted-base) case. This directly closes the only remaining
objection. **Convergence reached across all reviewers** — remaining items are deferred Architecture
concerns, not vision-level issues.
